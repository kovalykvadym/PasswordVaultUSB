<Window
    x:Class="PasswordVaultUSB.Views.IntrudersWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Intruder Log"
    Width="800"
    Height="450"
    MinWidth="500"
    MinHeight="250"
    Style="{StaticResource WindowStyle}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <ListBox
            Grid.Column="0"
            ItemsSource="{Binding IntruderImages}"
            SelectedItem="{Binding SelectedItem}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="5">
                        <TextBlock FontWeight="Bold" Text="{Binding TimestampDisplay}" />
                        <Image Height="60" Source="{Binding ImageSource}" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Grid Grid.Column="1" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Image
                Grid.Row="0"
                Source="{Binding SelectedItem.ImageSource}"
                Stretch="Uniform" />
            <Button
                Grid.Row="1"
                Margin="0,10,0,0"
                Padding="10,5"
                HorizontalAlignment="Right"
                Background="#FF5555"
                Command="{Binding DeleteCommand}"
                Content="Delete Photo"
                Foreground="White" />
        </Grid>
    </Grid>
</Window>