<Window x:Class="PasswordVaultUSB.Views.AddPasswordView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PasswordVaultUSB.Views"
        mc:Ignorable="d"
        Title="Add New Password" 
        Height="450" MinHeight="250"
        Width="800" MinWidth="600"
        WindowStartupLocation="CenterScreen"
        Style="{StaticResource WindowStyle}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Service:" Grid.Row="0"/>
        <TextBox x:Name="ServiceInput" Grid.Row="1"/>

        <TextBlock Text="Login:" Grid.Row="2"/>
        <TextBox x:Name="LoginInput" Grid.Row="3"/>

        <TextBlock Text="Password:" Grid.Row="4"/>
        <PasswordBox x:Name="PasswordInput" Grid.Row="5"/>
        
        <Button Content="Add" Grid.Row="6" Margin="0 20 0 0" Width="100" HorizontalAlignment="Center" Click="AddButton_Click"/>

    </Grid>
</Window>
