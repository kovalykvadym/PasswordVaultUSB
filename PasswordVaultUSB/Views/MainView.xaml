<Window
    x:Class="PasswordVaultUSB.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:PasswordVaultUSB.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:PasswordVaultUSB.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="MainView"
    Width="900"
    Height="600"
    MinWidth="700"
    MinHeight="450"
    PreviewKeyDown="ResetAutoLockTimer"
    PreviewMouseMove="ResetAutoLockTimer"
    Style="{StaticResource WindowStyle}"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">

    <Grid Background="{StaticResource WindowBackgroundBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="{StaticResource SidebarBackgroundBrush}">
            <StackPanel>
                <TextBlock
                    Margin="20,30,0,40"
                    Foreground="{StaticResource TextInverseBrush}"
                    Style="{StaticResource PageTitleStyle}"
                    Text="PVault" />

                <Button
                    Height="40"
                    Margin="15,0,15,30"
                    Click="AddPassword_Click"
                    Content="+ Add Password"
                    Style="{StaticResource ModernButtonStyle}" />

                <TextBlock
                    Margin="20,0,0,10"
                    FontSize="12"
                    FontWeight="Bold"
                    Foreground="{StaticResource TextSecondaryBrush}"
                    Text="MAIN MENU" />

                <Button
                    x:Name="MyPasswordsButton"
                    Background="{StaticResource SidebarActiveBackgroundBrush}"
                    Click="MenuButton_Click"
                    Content="My Password"
                    Foreground="{StaticResource TextInverseBrush}"
                    Style="{StaticResource MenuButtonStyle}" />

                <Button
                    x:Name="FavoritesButton"
                    Click="MenuButton_Click"
                    Content="Favorites"
                    Style="{StaticResource MenuButtonStyle}" />

                <Button
                    x:Name="UsbButton"
                    Click="MenuButton_Click"
                    Content="USB Security Key"
                    Style="{StaticResource MenuButtonStyle}" />

                <TextBlock
                    Margin="20,20,0,10"
                    FontSize="12"
                    FontWeight="Bold"
                    Foreground="{StaticResource TextSecondaryBrush}"
                    Text="SYSTEM" />

                <Button
                    x:Name="SettingsButton"
                    Click="MenuButton_Click"
                    Content="Settings"
                    Style="{StaticResource MenuButtonStyle}" />
                <Button
                    Command="{Binding LockVaultCommand}"
                    Content="Lock Vault"
                    Style="{StaticResource MenuButtonStyle}" />
            </StackPanel>
        </Border>

        <Grid Grid.Column="1" Margin="30">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid
                x:Name="HeaderGrid"
                Grid.Row="0"
                Margin="0,0,0,20">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="250" />
                </Grid.ColumnDefinitions>


                <StackPanel Grid.Column="0">
                    <TextBlock Style="{StaticResource PageTitleStyle}" Text="My Passwords" />
                    <TextBlock
                        FontSize="14"
                        Style="{StaticResource DescriptionTextStyle}"
                        Text="Manage and secure your credentials" />
                </StackPanel>

                <Border
                    Grid.Column="1"
                    Padding="10,5"
                    VerticalAlignment="Center"
                    Background="White"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="15">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <TextBlock
                            Margin="0,0,5,0"
                            VerticalAlignment="Center"
                            Foreground="{StaticResource TextHintBrush}">
                            <Image
                                Width="20"
                                Height="20"
                                Source="/Resources/search.png" />
                        </TextBlock>

                        <TextBox
                            x:Name="SearchBox"
                            Grid.Column="1"
                            FontSize="14"
                            Style="{StaticResource BaseInputStyle}"
                            Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />

                        <TextBlock
                            Grid.Column="1"
                            Margin="2,0,0,0"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            Foreground="{StaticResource BorderBrush}"
                            IsHitTestVisible="False"
                            Text="Search...">

                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Text, ElementName=SearchBox}" Value="">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>

                        </TextBlock>
                    </Grid>
                </Border>
            </Grid>

            <StackPanel
                x:Name="ChangePasswordPanel"
                Grid.Row="1"
                Margin="0,0,0,20"
                Background="White"
                Orientation="Vertical"
                Visibility="Collapsed">

                <TextBlock
                    FontSize="18"
                    Style="{StaticResource SectionTitleStyle}"
                    Text="Change master password" />

                <StackPanel Margin="0,10,0,10">
                    <TextBlock
                        Foreground="{StaticResource TextSecondaryBrush}"
                        Style="{StaticResource SubHeaderStyle}"
                        Text="Current password" />
                    <controls:BindablePasswordBox Height="36" Password="{Binding CurrentPasswordInput, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>

                <StackPanel Margin="0,0,0,10">
                    <TextBlock
                        Foreground="{StaticResource TextSecondaryBrush}"
                        Style="{StaticResource SubHeaderStyle}"
                        Text="New password" />
                    <controls:BindablePasswordBox Height="36" Password="{Binding NewPasswordInput, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>

                <StackPanel Margin="0,0,0,15">
                    <TextBlock
                        Foreground="{StaticResource TextSecondaryBrush}"
                        Style="{StaticResource SubHeaderStyle}"
                        Text="Confirm new password" />
                    <controls:BindablePasswordBox Height="36" Password="{Binding ConfirmPasswordInput, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>

                <Button
                    Width="140"
                    Height="34"
                    HorizontalAlignment="Right"
                    Command="{Binding ChangeMasterPasswordCommand}"
                    Content="Update"
                    Style="{StaticResource ModernButtonStyle}" />

                <Separator Margin="0,15,0,10" />
                <TextBlock
                    Margin="0,0,0,5"
                    Foreground="{StaticResource TextSecondaryBrush}"
                    Style="{StaticResource SubHeaderStyle}"
                    Text="Activity log" />
                <Border
                    Height="150"
                    Background="{StaticResource LogBackgroundBrush}"
                    BorderBrush="{StaticResource LightBorderBrush}"
                    BorderThickness="1"
                    CornerRadius="6">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ItemsControl x:Name="ActivityLogList" ItemsSource="{Binding ActivityLog}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock
                                        Margin="6,2"
                                        FontSize="13"
                                        Foreground="#444"
                                        Text="{Binding}" />
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Border>
            </StackPanel>

            <DataGrid
                x:Name="PasswordsGrid"
                Grid.Row="2"
                Style="{StaticResource VaultDataGridStyle}">

                <DataGrid.Columns>
                    <DataGridTextColumn
                        Width="*"
                        Binding="{Binding Service}"
                        FontWeight="Bold"
                        Header="Service">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Center" />
                                <Setter Property="VerticalAlignment" Value="Center" />
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn
                        Width="*"
                        Binding="{Binding Login}"
                        Header="Login">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Center" />
                                <Setter Property="VerticalAlignment" Value="Center" />
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>


                    <DataGridTemplateColumn Width="150" Header="Password">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                                    <TextBlock
                                        Width="100"
                                        FontFamily="Consolas"
                                        FontSize="14"
                                        Text="{Binding DisplayPassword}"
                                        TextAlignment="Center"
                                        TextTrimming="CharacterEllipsis" />

                                    <Button
                                        Click="ToggleShowPassword_Click"
                                        Style="{StaticResource IconButtonStyle}"
                                        ToolTip="Show/Hide">
                                        <Image
                                            Width="18"
                                            Height="18"
                                            Opacity="{Binding EyeIconOpacity}"
                                            Source="/Resources/visibility.png" />
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn
                        Width="*"
                        Binding="{Binding Url}"
                        Foreground="{StaticResource LinkBrush}"
                        Header="Website">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Center" />
                                <Setter Property="VerticalAlignment" Value="Center" />
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTemplateColumn Width="120" Header="Actions">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">

                                    <Button
                                        Margin="0,0,5,0"
                                        Command="{Binding DataContext.CopyPasswordCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                        CommandParameter="{Binding}"
                                        Style="{StaticResource IconButtonStyle}"
                                        ToolTip="Copy to Clipboard">

                                        <Image
                                            Width="18"
                                            Height="18"
                                            Source="/Resources/copy.png" />
                                    </Button>

                                    <Button
                                        Margin="0,0,5,0"
                                        Click="EditPassword_Click"
                                        Style="{StaticResource IconButtonStyle}"
                                        ToolTip="Edit Record">
                                        <Image
                                            Width="18"
                                            Height="18"
                                            Source="/Resources/edit.png" />
                                    </Button>

                                    <Button
                                        Margin="0,0,5,0"
                                        Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                        CommandParameter="{Binding}"
                                        Style="{StaticResource IconButtonStyle}"
                                        ToolTip="Delete Record">

                                        <Image
                                            Width="18"
                                            Height="18"
                                            Source="/Resources/trash_can.png" />
                                    </Button>

                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Width="80" Header="Fav">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button
                                    Command="{Binding DataContext.ToggleFavoriteCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                    CommandParameter="{Binding}"
                                    Style="{StaticResource IconButtonStyle}">

                                    <Image
                                        Width="18"
                                        Height="18"
                                        Opacity="{Binding FavoriteIconOpacity}"
                                        Source="/Resources/star.png" />
                                </Button>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</Window>